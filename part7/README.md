# Part 7 — Production Systems

**This part has no code of its own.** Like [part 6](../part6), everything it teaches was
built inside the sprint project instead.

**The code lives in a separate repository: https://github.com/pehhalt/overprint-shop**
Live shop: https://overprint-shop.vercel.app · Preview: https://overprint-staging.vercel.app

---

## Coverage: not yet written

This file is a placeholder. The coverage analysis is written the same way part 6's was —
against the lesson's actual text, listing what the sprint project covers, what it leaves
open, and what was learned that the lesson does not mention.

It is deliberately being written **last**, after the work it describes is finished, so that
it records what actually happened rather than what was intended. Day 3 (rollback rehearsal,
go-live plan, evidence pack) is still in progress at the time of writing, and several of its
items are squarely part-7 material.

The raw material already exists: day 1 alone produced a two-environment deployment pipeline,
migration-managed schema, and a catalogue of five distinct ways a deploy can fail silently —
all recorded with evidence.
